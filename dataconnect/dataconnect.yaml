specVersion: "v1beta"
serviceId: "growell"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "growell-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
